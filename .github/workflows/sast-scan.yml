name: Security SAST scan (Detect vulnerabilities)

on:
  push:
    branches:
      - master
      - develop
      - preprod
      - 'feat/**'

jobs:
  sast_scan:
    uses: Xanatrox-Corp/orga-reusable-worfkflows/.github/workflows/security-reusable-workflows.yml@v1
