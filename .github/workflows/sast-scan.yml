name: Security SAST scan (Detect vulnerabilities)

on:
  push:
    branches:
      - main
      - develop
      - preprod
      - 'feat/**'

jobs:
  sast_scan:
    uses: Xanatrox-Corp/orga-reusable-workflows/.github/workflows/security-reusable-workflows@v1
